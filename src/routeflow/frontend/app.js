// Scaffold only — proves the static pipeline serves JS correctly.
// The real frontend (sidebar, node graph, WebSocket subscription) lands
// across the rest of Phase 6.
console.log("routeflow: flow view scaffold loaded");
